/* global expect:true */
/* global shallow:true */
/* global mount:true */

import React from 'react';// eslint-disable-line no-unused-vars

// import App from '../../src/components/App.component';

describe('<App />', () => {
  it('should have a box', () => {
    // const wrapper = shallow(
    //   <App />
    // );
    // expect(wrapper.find('#card-alert')).to.have.length(0);
  });
});
